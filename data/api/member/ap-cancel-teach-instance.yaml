post:
  summary: "Member: Attempt to cancel as instructor of an AP instance"
  tags:
  - Portal - Volunteer
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            instanceId:
              type: number
  responses:
    "200":
      description: "Success"
      content:
        application/json:
          schema:
            type: string
